import React, { useContext } from "react";
import useSaveCharacter from "../../context/CharacterContext";

const CharacterList: React.FC = () => {
  return (
    <div className="row mr-4"> </div>
  );
};

export default CharacterList;
